import React from "react";
import {Container} from "@mui/material";

const PeoplePage = () => {
  return (
    <Container
      disableGutters
      component="main"
      maxWidth={false}
      sx={{
        display: "flex",
      }}
    >
      Account
    </Container>
  );
};

export default PeoplePage;
